import { AlertInnerTextOnClickDirective } from './alert-inner-text-on-click.directive';

describe('AlertInnerTextOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertInnerTextOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
